<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_fill_black"
    android:layout_width="match_parent"
    android:layout_height="match_parent"

    tools:context="codetribe.co.za.brainblast.FillBlackActivity"
    >

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/btnSubmit"
        android:text="@string/submit_text"
        android:layout_below="@+id/etAnswer"
        android:onClick="onSubmit"
        android:layout_marginTop="10dp"
        android:background="@color/colorAccent"
        />

    <ImageView
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:id="@+id/ivImage"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:scaleType="centerCrop"
        android:layout_marginTop="5dp"/>

        <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/tvQuestion"
        android:textSize="@dimen/question_size"
        android:text="Temperature increases rapidly after_______"
        android:fontFamily="sans-serif-meduim"
        android:layout_below="@+id/ivImage"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:textStyle="bold"
        android:layout_marginTop="5dp"/>

        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/etAnswer"
            android:background="@drawable/corners"
            android:padding="10dp"
            android:inputType="text"
            android:layout_marginTop="12dp"
            android:layout_below="@+id/tvQuestion"
            android:layout_centerHorizontal="true" />
    </RelativeLayout>


</ScrollView>
